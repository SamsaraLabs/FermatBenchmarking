language: php
php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - nightly

before_install:
  - sudo apt-get update
  - sudo apt-get install libmpdec
  - pecl install decimal

install:
  - composer update

script:
  - ./bench.sh
